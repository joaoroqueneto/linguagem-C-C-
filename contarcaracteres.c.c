#include <stdio.h>
#include <string.h>

int main()
{
    char mensagem[] = "ola essa e uma mensagem de exmplo.";
    
    int comprimento = strlen(mensagem);
    
    printf("a mensagem tem %d caracteres.\n", comprimento);
    
    
  return 0;
}